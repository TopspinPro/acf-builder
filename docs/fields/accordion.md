# Accordion

## Method

```php
$group->addAccordion(string $label, array $args = [])
```

## Example

```php
$group->addAccordion('Advanced Settings', [
    'open' => 1,
    'multi_expand' => 0,
    'endpoint' => 0,
]);
```

## Common ACF Options

- `open`
- `multi_expand`
- `endpoint`
