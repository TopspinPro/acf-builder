# Color Picker

## Method

```php
$group->addColorPicker(string $name, array $args = [])
```

## Example

```php
$group->addColorPicker('background_color', [
    'default_value' => '#ffffff',
]);
```
